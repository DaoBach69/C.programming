#include <stdio.h>

int main(){
	
	int cnt, cnt1;
	cnt = cnt1 = 0;
	do
	{
		printf("\n Enter a numbers: ");
		scanf("%d", &cnt);
		printf("No. is %d", cnt);
		cnt1++;
	} while (cnt != );
	printf("\n The total numbers entered were %d", --cnt1);
	return 0;
	
}
